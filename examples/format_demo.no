bruk std.tekst som t

funksjon start() -> heltall {
    la navn: tekst = "Norscode"
    la tall = 1 + 2 * 3
    hvis (navn == "Norscode") {
        skriv(t.hilsen(navn))
        skriv(tekst_fra_heltall(tall))
    } ellers {
        skriv("feil")
    }
    returner 0
}
