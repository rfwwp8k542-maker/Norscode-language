bruk std.web som web

funksjon hjem(ctx: ordbok_tekst) -> ordbok_tekst {
    web.route("GET /")
    returner web.response_builder(200, {"content-type": "text/plain"}, "cors ok")
}

funksjon start() -> heltall {
    returner 0
}
