bruk std.audit som audit
bruk std.web som web

funksjon start() -> heltall {
    la e = audit.access_denied("ada", "admin", "/admin")
    audit.felt_tekst(e, "request_id", web.request_id(web.request_context("get", "/admin", {}, {}, "")))
    audit.emit(e)
    returner 0
}
