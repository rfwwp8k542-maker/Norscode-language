funksjon tom_tekst() -> liste_tekst {
    returner []
}

funksjon sjekk(l: liste_tekst) -> heltall {
    assert_eq(lengde(l), 0)
    returner 0
}

funksjon start() -> heltall {
    assert_eq(sjekk([]), 0)
    assert_eq(sjekk(tom_tekst()), 0)
    returner 0
}
