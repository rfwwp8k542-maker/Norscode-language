#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
MAIN_SCRIPT="${ROOT_DIR}/../main.py"

if [ ! -f "${MAIN_SCRIPT}" ]; then
  printf 'Fant ikke bootstrap-script: %s\n' "${MAIN_SCRIPT}" >&2
  exit 1
fi

exec /usr/bin/env python3 "${MAIN_SCRIPT}" "$@"
