# CSRF-hjelpere

funksjon token(ctx: ordbok_tekst) -> tekst {
    returner csrf_token(ctx)
}

funksjon require(ctx: ordbok_tekst, expected: tekst) -> bool {
    returner csrf_require(ctx, expected)
}
