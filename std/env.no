# Miljøvariabler

funksjon hent(nokkel: tekst) -> tekst {
    returner miljo_hent(nokkel)
}

funksjon finnes(nokkel: tekst) -> bool {
    returner miljo_finnes(nokkel)
}

funksjon sett(nokkel: tekst, verdi: tekst) -> tekst {
    returner miljo_sett(nokkel, verdi)
}
