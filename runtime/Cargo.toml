[package]
name = "norscode-runtime"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
postgres = "0.19"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
sha2 = "0.10"
