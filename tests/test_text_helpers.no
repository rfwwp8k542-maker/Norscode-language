bruk std.tekst som t

funksjon start() -> heltall {
    assert(t.starter_med("norscode", "nors"))
    assert(t.slutter_med("norscode", "code"))
    assert(t.inneholder("norscode", "scode"))
    assert_eq(t.trim("  hei  "), "hei")
    returner 0
}
