funksjon start() -> heltall {
    la person: ordbok_tekst = {"navn": "Ada", "rolle": "utvikler"}
    la navn = person.navn
    la rolle = person.rolle
    assert_eq(navn, "Ada")
    assert_eq(rolle, "utvikler")
    returner 0
}
